function findIndex(arr,value){
    return arr.indexOf(value);
}
// Example usage:
const colors = ['red', 'green', 'blue'];
console.log(findIndex(colors, 'green')); // Output: 1
console.log(findIndex(colors, 'yellow')); // Output: -1